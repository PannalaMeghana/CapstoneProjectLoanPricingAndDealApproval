# Server Configuration
server.port=8080
spring.application.name=loan-approval-system

# MongoDB Configuration
spring.data.mongodb.uri=mongodb://localhost:27017/loan_approval_db
spring.data.mongodb.database=loan_approval_db

# JWT Configuration
jwt.secret=YourSecretKeyForJWTTokenGenerationMustBeLongEnoughForHS512Algorithm
jwt.expiration=86400000

# CORS Configuration
cors.allowed-origins=http://localhost:4200

# Logging
logging.level.com.corporate.banking=DEBUG
logging.level.org.springframework.security=DEBUG
